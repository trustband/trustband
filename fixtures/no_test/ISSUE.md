# Bug: is_even() returns the wrong result

`is_even(n)` returns True for odd numbers and False for even ones. There is no
test covering it yet — the Reproducer should author a failing test before the fix.
