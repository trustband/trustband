"""TrustBand: a band of agents that turns a bug/issue into a trustworthy fix PR.

The differentiator is the Verifier agent, which earns the merge with real-path
tests, regression checks and trajectory assertions rather than just writing code.
"""

__version__ = "0.1.0"
